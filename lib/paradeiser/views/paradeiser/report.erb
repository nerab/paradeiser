ID | Name | Status | Started | Ended
<% @pom.each do |pom| %>
<%= pom.id %> | <%= pom.name %> | <%= pom.status %> | <%= pom.started_at.strftime('%R') %> | <%= pom.finished_at.strftime('%R') if pom.finished? %>

<% end %>
